#will connect to the server on TCP port 23456

#prompt for keyboard input.

#“QUIT”, the client program will close the TCP connection to the server and it will then exit normally through the bottom of the program (no break or immediate quit).

#“TIME” in the client program, the command will be sent to the server and the server will return its system time, then the client will display the servers system time.